// Leftover code to correctly display all color changes.
                
    // var gameBoard = JSON.parse(response);
    // $(gameBoard).each(function(coord1, largeCell) {
    //   $(largeCell).each(function(coord2, smallCell)  {
    //         if (smallCell !== "") {
    //             $("label[for='" + coord + "']");
    //             $("label[for='" + coord1 + coord2 + "']").addClass("p" + smallCell);
    //         }
    //   });
    // });